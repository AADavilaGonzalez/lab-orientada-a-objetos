# PIA de Lab Orientado a Objetos

Este proyecto esta hecho con el build tool maven a comparacion
con el resto del repositorio que esta hecho con gradle

para correrlo individualmente ejectuar el comando
`mvn compile exec:java` con maven instalado y en el PATH
